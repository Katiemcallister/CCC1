<!-- index.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Job Role Information</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <h1>Job Role Information</h1>

    <% if (jobRole) { %>
        <table>
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Level</th>
                    <th>Capability</th>
                    <th>Description</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td><%= jobRole.name %></td>
                    <td><%= jobRole.level %></td>
                    <td><%= jobRole.capabilities %></td>
                    <td><%= jobRole.description %></td>
                </tr>
            </tbody>
        </table>
    <% } else { %>
        <p>No job role found.</p>
    <% } %>

    
</body>
</html>
